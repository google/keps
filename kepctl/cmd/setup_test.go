package cmd_test

import (
	. "github.com/onsi/ginkgo"
	_ "github.com/onsi/gomega"
)

var _ = Describe("setting up kepctl commands", func() {
	Describe("Setup()", func() {
		It("prepares a keps.Instance, an orgs.Instance, and a settings.Runtime for use by commands", func() {

			Fail("need to fix up the settings package")
			Fail("test not written")

		})
	})
})
