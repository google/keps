package settings_test

import (
	. "github.com/onsi/ginkgo"
	. "github.com/onsi/gomega"
)

var _ = Describe("Opening settings for the KEP tool", func() {
	Describe("Open()", func() {
		It("reads setting information from disk", func() {
			Fail("test not written")
		})

		Context("when the settings file does not exist", func() {
			Fail("test not written")
		})

		Context("when the GitHub handle is not set in the settings file", func() {
			Fail("test not written")
		})

		Context("when the content root is not set in the settings file", func() {
			Fail("test not written")
		})

		Context("when the token location is not set in the settings file", func() {
			Fail("test not written")
		})
	})
})
